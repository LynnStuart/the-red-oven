backend:
  name: git-gateway
  branch: master # Branch to update (optional; defaults to master)

# This line should *not* be indented
publish_mode: editorial_workflow

# These lines should *not* be indented
media_folder: "static/images/uploads" # Media files will be stored in the repo under static/images/uploads
public_folder: "/images/uploads" # The src attribute for uploaded media will begin with /images/uploads

collections:
  - name: "blog" # Used in routes, e.g., /admin/collections/blog
    label: "Blog" # Used in the UI
    folder: "content/posts" # The path to the folder where the documents are stored
    create: true # Allow users to create new documents in this collection
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}/index.md" # Filename template, e.g., YYYY-MM-DD-title.md
    fields: # The fields for each document, usually in front matter
      - {label: "Title", name: "title", widget: "string"}
      - {label: "Publish Date", name: "date", widget: "datetime"}
      - {label: "Description", name: "description", widget: "string"}
      - {label: "Drop Cap", name: "dropCap", widget: "boolean", required: false, default: false}
      - {label: "Display in Menu", name: "displayInMenu", widget: "boolean", required: false, default: false}
      - {label: "Display in List", name: "displayInList", widget: "boolean", required: false, default: false}
      - {label: "Draft", name: "draft", widget: "boolean", required: false, default: true}
      - {label: "Categories", name: "categories", widget: "list", required: false}
      - {label: "Featured Image", name: "thumbnail", widget: "image", required: false}
      - {label: "Rating (scale of 1-5)", name: "rating", widget: "number", required: false}
      - {label: "Body", name: "body", widget: "markdown"}
      - label: "Resources"
        name: "resources"
        widget: "object"
        required: false
        fields:
          - {label: "Name", name: "name", widget: "string"}
          - label: "Source"
            name: "src"
            widget: "file"
            media_library:
              config:
                multiple: true

